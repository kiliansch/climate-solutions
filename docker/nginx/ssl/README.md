# SSL certificates directory

This directory is **not committed** to version control (see `.gitignore`).

Run `make ssl` (or `make install`) to generate a self-signed certificate for local development.
